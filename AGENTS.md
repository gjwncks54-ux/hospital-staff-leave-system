# Codex Workspace Instructions

Use the local `.codex/` configuration in this workspace as the default operating baseline.

Key expectations:

- Gather evidence before editing files.
- Prefer targeted search and focused reads over broad scans.
- Use available MCP servers when they materially help.
- For review tasks, prioritize correctness, security, regressions, and missing tests.
- For API or framework claims, verify against primary documentation before presenting them as facts.

Additional Codex-specific notes live in `.codex/AGENTS.md`.
